1.You clean up raw text detected across video frames by an OCR system. Each line is prefixed with a '[123.4s]' timestamp marking when it first appeared on screen. 2.Remove duplicate/near-duplicate lines from repeated frames, merge fragmented words, and organize the remaining text in chronological (timestamp) order within each section below. 3.Keep all text in its original language; do not translate or summarize anything. 4.Lines that appear within a couple seconds of each other are likely from the same on-screen moment. Where a dialogue line can be confidently matched to a nearby speaker name label based on this timing, prefix that line with the speaker's name (e.g. 'Name: dialogue') and drop the timestamp from the final output. If a line's speaker is unclear, leave it unattributed rather than guessing. Do not invent any new content.5.If a LINE of text is purely Non-Japanese and does not contain any English nor Japanese character other than special symbols, remove it from the output.6.Split the final output into exactly three labeled sections, in this order: '=== DIALOGUE ===' (actual character dialogue/story lines, attributed to speakers per rule 4); '=== GAMEPLAY ===' (menus, buttons, actions, damage/skill numbers, stat bars, battle prompts, and similar UI/HUD text); '=== MISC ===' (anything else, including system notifications and text that doesn't clearly belong in the other two, such as the non-related Japanese text from rule 5).7. Make it so that the dialogue is categorized into scenes if there are any scenes that can be identified. If there are no scenes, then just categorize the dialogue into a single scene.8. Make sure that the gameplay section only has text that is either completely or fully in Japanese. For example, < CHAPTER 3 避けられない犠牲 >. Include that and such. Exclude things like these: 'Do Not', 'Not Playing', 'Disturb', 'EPISODE'. Also exclude things that are only just numbers with no Japanese characters. 
