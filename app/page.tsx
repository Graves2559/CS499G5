"use client";
import { useState } from "react";
import { Plus, Upload, ChevronDown } from "lucide-react";
export default function Home() {
 const [input,setInput]=useState("Image");
 return <div className="app-shell"><header className="topbar"><a className="brand" href="/" aria-label="ClosedAI home"><span className="brand-orb"/>ClosedAI</a><button className="pill login">Login</button></header><div className="workspace"><aside className="sidebar" aria-label="Recent chats"><div className="sidebar-heading"><h2>Recent</h2><button className="pill"><Plus size={13}/>New chat</button></div></aside><main className="panels"><section className="panel" aria-labelledby="input-title"><div className="panel-heading"><h1 id="input-title">Input</h1><div className="select-wrap"><select aria-label="Input type" value={input} onChange={e=>setInput(e.target.value)}><option>Image</option><option>Texts</option></select><ChevronDown size={16}/></div></div><div className="input-body">{input==="Image"?<button className="dropzone"><Upload size={20} strokeWidth={1.5}/><span>Drop an image here, or click to browse</span><small>PNG, JPG up to 10MB</small></button>:<textarea className="text-input" aria-label="Text to summarize" placeholder="Type or paste your text here…"/>}<div className="actions"><button className="run-button">Run analysis</button></div></div></section><section className="panel output-panel" aria-labelledby="output-title"><div className="panel-heading"><h2 id="output-title">Output</h2><div className="select-wrap"><select aria-label="Output type"><option>Summary</option></select><ChevronDown size={16}/></div></div><div className="output-body"><div className="empty-state"><span className="empty-icon"><Plus size={20} strokeWidth={1.5}/></span><p>Nothing to show yet. Add an<br/> image or some text on the left<br/> and run analysis.</p></div></div></section></main></div></div>;
}
